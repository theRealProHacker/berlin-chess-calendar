# Phase 0 (build scripts + static site) needs ONLY the Python standard library.
# Nothing here is required to run `python -m bcc.build` or the tests.
#
# The packages below are for Phase 2+ (the FastAPI curation tool with the
# review/edit UI and the feed ingestion). They need a virtualenv:
#   sudo apt install python3.12-venv        # one-time, Debian/Ubuntu
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
pydantic>=2.7
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
httpx>=0.27
feedparser>=6.0
pyyaml>=6.0
